{
  "name": "diffrast-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Browser viewer for the differentiable rasterizer — watch a fit converge live",
  "type": "module",
  "scripts": {
    "wasm": "wasm-pack build ../crates/diffrast-wasm --target web --out-dir ../../web/pkg",
    "build": "tsc",
    "all": "npm run wasm && npm run build",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
